<h1>index</h1>


<div class="row">
  <div class="col-xs-6 col-xs-offset-3">

      <h2>Listed Galleries</h2>
    {{!-- if image artist id matches gallery creator id, display those images --}}
    {{#each galleries}}
      <ul>
        <li><a href="/galleries/{{this._id}}">{{this.title}}</a></li>
      </ul>
    {{/each}}

    <hr>

  </div>
</div>
